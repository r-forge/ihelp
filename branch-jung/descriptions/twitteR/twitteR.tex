install.packages("twitteR");
install.packages("ROAuth");
install.packages("tm");
install.packages("wordcloud");
install.packages("RCurl");
install.packages("KoNLP")

library(twitteR);
library(ROAuth);
library(tm);
library(wordcloud);
library(RCurl);

requestURL <- "https://api.twitter.com/oauth/request_token"
accessURL = "http://api.twitter.com/oauth/access_token"
authURL = "http://api.twitter.com/oauth/authorize"
consumerKey = "wayUUq3FzU8jsedralSHg"
consumerSecret = "JQrMidiFZuEkJoBsEnsu6Hzz4ys7AeuouT298YSs"
Cred <- OAuthFactory$new(consumerKey=consumerKey,
                         consumerSecret=consumerSecret,
                         requestURL=requestURL,
                         accessURL=accessURL, 
                         authURL=authURL)

#The next command provides a URL which you will need to copy and paste into your favourite browser
#Assuming you are logged into Twitter you will then be provided a PIN number to type into the R command line

# Checks that you are authorised(need own PIN number)

Cred$handshake(cainfo = system.file("CurlSSL", "cacert.pem", package = "RCurl") )

# Insert PIN number, then below..

registerTwitterOAuth(Cred)

# 이건 그 아이 정보 코드
# requestURL <- "https://api.twitter.com/oauth/request_token"
# accessURL = "http://api.twitter.com/oauth/access_token"
# authURL = "http://api.twitter.com/oauth/authorize"
# consumerKey = "hus3X1MaQ7znKNiU9313Jw"
# consumerSecret = "32m4DzXVkyEgTn9wbqes50NEylzUki2FrO6iCFYmg"
# Cred <- OAuthFactory$new(consumerKey=consumerKey,
                         consumerSecret=consumerSecret,
                         requestURL=requestURL,
                         accessURL=accessURL, 
                         authURL=authURL)



# or

cred <- OAuthFactory$new(consumerKey=consumerKey,
				 consumerSecret=consumerSecret,
				 requestURL=requestURL, 
				 accessURL=accessURL, 
				 authURL=authURL);
 
download.file(url="http://curl.haxx.se/ca/cacert.pem" , destfile="cacert.pem")
 
cred$handshake(cainfo = "cacert.pem");
 registerTwitterOAuth(cred);
 
rdmTweets <- userTimeline("saskorea", n=100 , cainfo="cacert.pem");
 rdmTweets [1:10];
 
sea <- searchTwitter('gdragon', n=100, cainfo="cacert.pem");
 sea[1:20];
 sources<-sapply(sea,function(x) x$getStatusSource());
 sources<-gsub("</a>","",sources);
 sources<-strsplit(sources,">");
 sources<-sapply(sources,function(x) ifelse(length(x) >1, x[2] , x[1]));
 pie(table(sources));
 
availTrends <- availableTrendLocations('beyonce', n=100, cainfo="cacert.pem")
 head(availTrends)
 https://api.twitter.com/
 
----------------------
 install.packages("twitteR")
 install.packages("KoNLP")
 install.packages("wordcloud")
 install.packages("plyr")

 # Library 로드
 library(twitteR)
 library(KoNLP)
 library(wordcloud)
 library(plyr)

 # 분석할 키워드 설정
 keyword <- "what"

 # 트위터에서 키워드로 검색, 시작날짜, 지역코드(우리나라만 적용), 가져올 개수를 옵션에 대입
 h3_twitter <- searchTwitter(keyword,since='2012-10-29',g

